<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Backup all shared preferences and settings -->
    <include domain="sharedpref" path="." />
    <!-- Exclude the local database from cloud backup (rebuilt from Firestore) -->
    <exclude domain="database" path="." />
</full-backup-content>